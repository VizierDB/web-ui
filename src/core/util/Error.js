/**
 * Simple object for error messages that are generated during asynchronous
 * processing.
 */

export class ErrorObject {
    constructor(title, message) {
        this.title = title
        this.message = message
    }
}
